module Factorix
  module Transfer
    class Downloader
      attr_reader download_cache: Cache::FileSystem
      attr_reader http: HTTP

      def initialize: (?download_cache: Cache::FileSystem, ?http: HTTP) -> void

      def download: (URI::HTTPS url, Pathname output) -> void
    end
  end
end
